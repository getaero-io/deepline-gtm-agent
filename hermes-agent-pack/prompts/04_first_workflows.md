# Prompt: Run First Draft-Only Workflows

```text
Run the first Deepline GTM workflow in draft-only mode.

Workflow options:

1. Post-call follow-up
2. Account research brief
3. LinkedIn post draft
4. Outbound angle map
5. CRM hygiene proposal

Before running:

- state which context files you are using
- state which tools you will use
- start a Deepline Session UI plan when using providers, enrichment, or workflows
- state what you will not do without approval

During running:

- use Deepline-native execution for GTM provider/workflow access
- update Deepline session status for live progress
- run one-row or draft-only pilots before bulk work

After running:

- provide the draft output
- list source files/tools used
- list Deepline run/output/log/usage references when available
- list claims that need review
- suggest the reusable skill this should become

Do not send, publish, or write externally.
```
